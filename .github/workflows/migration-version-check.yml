name: Migration Version Check

on:
  pull_request:
  push:
    branches:
      - main
      - master
      - mitchwork

jobs:
  migration-check:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: lokrobot
          POSTGRES_PASSWORD: lokrobot
          POSTGRES_DB: lokrobot
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U lokrobot -d lokrobot"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      POSTGRES_URL: postgresql://lokrobot:lokrobot@localhost:5432/lokrobot
      POSTGRES_HOST: localhost
      POSTGRES_PORT: 5432
      POSTGRES_DB: lokrobot
      POSTGRES_USER: lokrobot
      POSTGRES_PASSWORD: lokrobot
      POSTGRES_SSL: "false"
      SCHEMA_VERSION: "1"
      SCHEMA_VERSION_LABEL: "baseline-v1"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Apply migrations
        run: npm run migrate

      - name: Validate migration version
        run: npm run migrate:check
